// program to find maximum number between two numbers

#include <stdio.h>
#include <conio.h>
int main()
{
    int a, b;
    printf("Enter two numbers to check which of the number is greates\n");
    scanf("%d%d", &a, &b);
    if (a > b)
        printf("%d is the greatest\n", a);
    else
        printf("%d is the greatest\n", a);

    return 0;
}